fastapi==0.111.0
uvicorn[standard]==0.30.0
langchain==0.2.0
langchain-groq==0.1.3
langchain-core==0.2.0
pydantic==2.7.0
rouge-score==0.1.2
python-dotenv==1.0.0
httpx==0.27.0

# ML / Embeddings (already installed on this machine)
sentence-transformers>=2.7.0
torch>=2.0.0
transformers>=4.40.0
scikit-learn>=1.3.0
numpy>=1.24.0

# Vector DB (chromadb already installed)
chromadb>=0.5.0

# Graph memory
networkx>=3.0

# Optional: Mem0 semantic memory (pip install mem0ai)
# mem0ai>=0.1.0
